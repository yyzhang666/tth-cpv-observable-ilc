# notebooks/

Scratch space for exploration. Rules:

- Notebooks are for looking; results that matter graduate into `scripts/` +
  `src/` with config and metadata.
- Clear outputs before committing (`jupyter nbconvert --clear-output`).
- Read paths from `configs/` — same as everywhere else in the repo.
