# Diagnostics and historical investigations

Put targeted truth-matching, binning, frame, correlation, bootstrap, or
likelihood cross-checks here.  Such commands may consume maintained workflow
outputs, but maintained workflows must not import from this directory.

The complete 2026-09-07 working copies of the investigation scripts and their
outputs are preserved on NAF at:

```
/data/dust/user/zhangyuy/analysis/tth/yuyang_tth_observable/archive/
  yuyang_research_snapshot_20260907/
```

Those scripts intentionally remain a frozen reproduction snapshot: many have
absolute NAF defaults or answer one narrow audit question.  Promote a useful
operation into `src/ilc_tth_cpv/` plus a thin `scripts/workflows/` entry only
after its input schema and physics contract are made general.

The result-to-producer map is in
`docs/YUYANG_RESULT_CATALOG_20260907.md`.
